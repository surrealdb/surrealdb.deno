{
  "name": "suurealdb-js",
  "version": "0.0.1",
  "description": "The official SurrealDB library for JS / TS.",
  "types": "./dist/types.d.ts",
  "main": "./dist/node.cjs.js",
  "module": "./dist/browser.esm.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && rollup -c"
  },
  "devDependencies": {
    "esbuild": "^0.15.7",
    "rollup": "^2.79.0",
    "rollup-plugin-dts": "^4.2.2",
    "rollup-plugin-esbuild": "^4.10.1"
  },
  "dependencies": {
    "ws": "^8.8.1"
  }
}
